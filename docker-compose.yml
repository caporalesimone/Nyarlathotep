services:
  nyarlathotep:
    container_name: nyarlathotep
    image: nyarlathotep:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    networks:
      - nyarlathotep-network
    restart: always

networks:
  nyarlathotep-network:
    driver: bridge
